## Packages
framer-motion | For beautiful card entry animations, progress bar filling, and strike-through transitions
canvas-confetti | For a delightful micro-interaction when all daily tasks are completed
@types/canvas-confetti | TypeScript definitions for canvas-confetti
lucide-react | Icons for UI elements

## Notes
Tailwind Config - Please ensure the following fonts are loaded if not already:
We are using custom CSS variables for fonts mapped to standard HTML tags to avoid needing tailwind.config.ts changes.
No other special integration notes. The frontend relies on the @shared/routes API manifest.
